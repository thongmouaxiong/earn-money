import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.febird.earn_money',
  appName: 'Earn Money',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
